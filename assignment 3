#include<stdio.h>
void main()
{
	int n,i,j,flag;
	int a[20];
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		for(j=2;i<=a[i];j++)
		{
			if(a[i]%j==0)
			{
				flag=1;
			}
		}
		if(flag==0)
		printf("%d",a[i]);
	}
	}
